// don't understand post-draw completely
// but it executes after everything is rendered on the screen ig

//also try placing this code in step event

if (place_meeting(x, y, oWall)) instance_destroy();